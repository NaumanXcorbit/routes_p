<% if @book.errors.any? %>
$('#error_area').html("<%= escape_javascript render('error', book: @book) %>");
<% else %>
$('#booksTable').prepend("<%= escape_javascript render('book_row', book: @book) %>");
$('.close_modal').click();
<% end %>